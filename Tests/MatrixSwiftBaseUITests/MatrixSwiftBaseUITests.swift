import XCTest
@testable import MatrixSwiftBaseUI

final class MatrixSwiftBaseUITests: XCTestCase {

    func testModuleCompiles() {
        // Placeholder so the test target builds. UI tests live in consumer apps
        // for now; this exists to verify MatrixSwiftBaseUI imports cleanly.
        XCTAssertTrue(true)
    }
}
