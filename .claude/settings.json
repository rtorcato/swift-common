{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.swift) command -v swiftlint >/dev/null 2>&1 && swiftlint --fix --quiet \"$f\" ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
